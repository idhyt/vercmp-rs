[package]
name = "rpm-vercmp"
version = "0.1.0"
edition = "2024"
description = "RPM version comparison crate"
license = "Apache-2.0"

[dependencies]

[build-dependencies]
ws-build = { path = "../ws-build" }

[features]
default = []
generate-bindings = ["ws-build/generate-bindings"]
