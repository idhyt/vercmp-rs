[package]
name = "rpm-version"
version = "0.1.0"
edition = "2024"

[dependencies]

[build-dependencies]
ws-build = { path = "../ws-build" }

[features]
default = []
generate-bindings = ["ws-build/generate-bindings"]
